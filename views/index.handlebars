<h1> Scraper HW (Giant FAIL)</h1>

<div class="container">
    <button class="btn btn-primary">Save Article</button>
    <button class="btn btn-primary">Scrape Articles</button>
</div>

<div class="container">
    {{#each articles}}
    <h2>{{title}}</h2>
    <a href="{{link}}" target="_blank">{{link}}</a>
    {{/each}}
</div>



